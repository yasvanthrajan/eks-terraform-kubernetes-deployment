apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx        # ← this label is what the Service uses to find this pod
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80